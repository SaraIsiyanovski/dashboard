const Icon1 = ({ name }) => {
  return <span className="icon icon1">{name}</span>;
};

const Icon2 = ({ name }) => {
  return <span className="icon icon2">{name}</span>;
};

export { Icon1, Icon2 };
